<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="fr.univcotedazur.iut.info.m414.projet">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.ProjetM414">
        <activity android:name=".AdditionLessonActivity" />
        <activity android:name=".ExercicesActivity" />
        <activity android:name=".LessonsActivity" />
        <activity android:name=".CreateExerciceActivity"/>
        <activity android:name=".CreateSpecificExerciceActivity"/>
        <activity android:name=".MultiplicationLessonActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--activity android:name=".calculinputview.CalculActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-->
    </application>

</manifest>
